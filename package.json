{
  "name": "vue-auto-fetch",
  "version": "1.0.0",
  "description": "A Vue.js library for auto-fetching data",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/index.js",
    "dev": "tsc --watch",
    "test": "echo \"No tests yet...\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^4.5.5",
    "@types/node": "^16.0.0"
  },
  "dependencies": {
    "vue": "^3.2.0" 
  }
}
